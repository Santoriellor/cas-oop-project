/**
 * Production environment configuration.
 *
 * <p>
 * This file contains environment-specific settings that are used
 * when the application is built and deployed in production mode.
 * </p>
 */
export const environment = {

  /**
   * Indicates that the application is running in production mode.
   */
  production: true,

  /**
   * Base URL of the backend API used in production.
   */
  apiUrl: 'https://casproject.santoriello.ch/api'
};
